@using System
@using AIP.GUI
<table width="100%" frame="border" align="center">
    <colgroup>
        <col width="4%" />
        <col width="48%" />
        <col width="48%" />
    </colgroup>
    <tbody>
        <tr>
            <th align="left" class="bleft btop bright bbottom" width="10">1</th>
		<th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_1"))</th>
            <td class="bleft btop bright bbottom">@Raw(Model.HeliportType)</td>
        </tr>
        <tr>
            <th align="left" class="bleft btop bright bbottom">2</th>
	  <th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_2"))</th>
            <td class="bleft btop bright bbottom">
                @foreach (var data in Model.HeliportData)
                {
                    <text>@Raw(data["TLOFDimensions"]) (@Raw(data["Designator"]))<br /></text>
                }
            </td>
        </tr>
        <tr>
            <th align="left" class="bleft btop bright bbottom">3</th>
	  <th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_3"))</th>
            <td class="bleft btop bright bbottom">@Raw(Model.FATOTrueBearings)</td>
        </tr>
        <tr>
            <th align="left" class="bleft btop bright bbottom">4</th>
	  <th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_4"))</th>
            <td class="bleft btop bright bbottom">
                @foreach (var data in Model.HeliportData)
                {
                    <text>@Raw(data["SurfaceType"]) (@Raw(data["Designator"]))<br /></text>
                }
            </td>
        </tr>
        <tr>
            <th align="left" class="bleft btop bright bbottom">5</th>
	  <th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_5"))</th>
            <td class="bleft btop bright bbottom">
                @foreach (var data in Model.HeliportData)
                {
                    <text>@Raw(data["BRGStrength"]) (@Raw(data["Designator"]))<br /></text>
                }
            </td>
        </tr>
        <tr>
            <th align="left" class="bleft btop bright bbottom">6</th>
	  <th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_6"))</th>
            <td class="bleft btop bright bbottom">
                @foreach (var data in Model.HeliportData)
                {
                    <text>@Raw(data["TLOFCoordinates"]) (@Raw(data["Designator"]))<br /></text>
                }
            </td>
        </tr>
        <tr>
            <th align="left" class="bleft btop bright bbottom">7</th>
	  <th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_7"))</th>
            <td class="bleft btop bright bbottom">
                @foreach (var data in Model.HeliportData)
                {
                    <text>@Raw(data["TLOFElevation"]), @Raw(data["TLOFSlope"]) (@Raw(data["Designator"]))<br /></text>
                }
            </td>
        </tr>
        <tr>
            <th align="left" class="bleft btop bright bbottom">8</th>
	  <th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_8"))</th>
            <td class="bleft btop bright bbottom">@Raw(Model.SafetyArea)</td>
        </tr>
        <tr>
            <th align="left" class="bleft btop bright bbottom">9</th>
	  <th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_9"))</th>
            <td class="bleft btop bright bbottom">@Raw(Model.HELCWYDimensions)</td>
        </tr>
        <tr>
            <th align="left" class="bleft btop bright bbottom">10</th>
	  <th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_10"))</th>
            <td class="bleft btop bright bbottom">@Raw(Model.ObstacleFreeSector)</td>
        </tr>
        <tr>
            <th align="left" class="bleft btop bright bbottom">11</th>
      <th align="left" class="bleft btop bright bbottom">@Raw(@Tpl.Text("AD312_11"))</th>
            <td class="bleft btop bright bbottom">@Raw(Model.Remarks)</td>
        </tr>
    </tbody>
</table>