<Window x:Class="ChartManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:ChartManager.View"
        xmlns:viewModel="clr-namespace:ChartManager.ViewModel"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="500" Width="1200">
    <Window.Resources>

        <DataTemplate DataType="{x:Type viewModel:InitialViewModel}">
            <view:InitialView/>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type viewModel:HistoryViewModel}">
            <view:History/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ContentControl Content="{Binding ViewModel}" />
    </Grid>
</Window>