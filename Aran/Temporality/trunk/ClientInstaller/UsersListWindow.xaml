<Window x:Class="ClientInstaller.UsersListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ViewModel="clr-namespace:ClientInstaller.ViewModel"
        xmlns:local="clr-namespace:ClientInstaller"
        mc:Ignorable="d"
        Title="Users" Height="422" Width="396"
        WindowStyle="ToolWindow" VerticalAlignment="Center" HorizontalAlignment="Center" ResizeMode="NoResize"
        Name="ConfigNameControl">
    <Window.DataContext>
        <ViewModel:UsersListViewModel/>
    </Window.DataContext>
    <Grid Background="Lavender" Margin="2,1,2,0">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30px"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Row="0" Grid.Column="0" 
                 ItemsSource="{Binding Path=AllUsers}" SelectedItem="{Binding Path=SelectedUser}" DisplayMemberPath="Name"/>
        <Button Grid.Row="1" Grid.Column="0" Margin="5px" Content="Select" Command="{Binding Path=SelectCommand}" CommandParameter="{Binding ElementName=ConfigNameControl}"/>
    </Grid>
</Window>
